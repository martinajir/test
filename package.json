{
  "name": "recipe-repository",
  "version": "1.0.0",
  "description": "A recipe repository API backed by a SQLite database (using Node's built-in node:sqlite module).",
  "main": "src/server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "migrate": "node scripts/migrate.js",
    "seed": "node scripts/seed.js",
    "setup": "npm run migrate && npm run seed",
    "test": "node --test test/*.test.js"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.21.0"
  }
}
